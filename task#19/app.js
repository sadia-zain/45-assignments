"use strict";
let guests_list = ["sadia", "saira", "iqra"];
for (let i = 0; i < guests_list.length; i++) {
    console.log(`i am inviting , ${guests_list[i]} people to dinner.`);
}
console.log(`i an inviting total, ${guests_list.length} people`);
